fn main() {
    //    let configmax = Some(3u8);
    //        match configmax {
    //            Some(max) => println!("the maximum is configured to be {max}"),
    //            _ => println!("none value"),
    //        }

    // instead of using match like this we can use `if let`

    //    let config_max = Some(3u8);
    //    if let Some(max) = config_max {
    //    println!("the maximum is configured to the max {max}");
    //    }
}
